import 'package:flutter/material.dart';

class DragAndDropPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Drag and drop')),
      body: Center(child: Text('Drag and drop content')),
    );
  }
}
