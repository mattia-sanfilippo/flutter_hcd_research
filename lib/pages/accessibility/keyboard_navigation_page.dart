import 'package:flutter/material.dart';

class KeyboardNavigationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Keyboard navigation')),
      body: Center(child: Text('Keyboard navigation content')),
    );
  }
}